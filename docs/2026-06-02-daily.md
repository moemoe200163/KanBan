# 2026-06-02 Daily Log

## 今日成果

### Commit: `b565099` — P0 Hardening

Code review 發現 2 HIGH / 2 MEDIUM / 1 LOW，全部修復：

| Issue | 改動 | 驗證 |
|-------|------|------|
| **H1** DB init 吞異常 | `main.py` lifespan: `logger.warning` → `logger.exception` + `raise`，app fail fast | 35 tests pass |
| **H2** ProcessRunner init 吞異常 | 同上，不再設 `app.state.runner = None` | 同上 |
| **M2** `_db_initialized` TOCTOU 競態 | `database.py` 新增 `asyncio.Lock`，`ensure_db_init()` 用 `async with` 保護 | 同上 |
| **M1** `get_session` dead code | 移除未使用的 FastAPI dependency | — |
| **L1** ecc.py auth no-op | 加 `# TODO(P1)` 註記，明確標記需在 production 前補 JWT middleware | — |

### Gate 結果

```
pytest:        35 passed
nuxi typecheck: exit 0
nuxi build:    407 kB gzip, Build complete
```

---

## Code Review 總結

用 `code-reviewer` 子代理審查了 13 個檔案，6 個指定檢查點全 CLEAN：

1. `asyncio.to_thread` 放置與例外處理 — 正確
2. Migration vs Model 7 張表欄位對齊 — 無 drift
3. `/health/ready` DB 正常回 200 — 正確且有測試覆蓋
4. `test_reset` 雙重 gating — 路由掛載 + 請求時雙重檢查
5. Docker `NUXT_PUBLIC_API_BASE` — build arg/runtime env 一致
6. 無殘留引用已刪除檔案 — 零匹配

---

## Known Problems 更新

原 `CLAUDE.md` 列了 6 個 Known Problems，今日修復狀態：

| # | 問題 | 狀態 |
|---|------|------|
| 1 | Board 問題 | 已在 `3f9f8a7` 修復（seed + repository） |
| 2 | DB init warning | **今日 H1 修復** — app 不再吞異常，fail fast |
| 3 | Smoke tests fail | 已在先前 session 修復，35 tests 全過 |
| 4 | ECC dispatch 太接近 real adapter | 已改為 safe runner（`_execute_safe_runner`） |
| 5 | Playwright 未安裝 | **未修** — 仍需手動確認 |
| 6 | 重複 sidebar 路徑 | **未修** — P1 待處理 |

---

## 下一步規劃

### P1 — 近期待辦

| 項目 | 說明 | 依賴 |
|------|------|------|
| E2E setup | 安裝 Playwright，讓 `npm run e2e` 可用 | 無 |
| Sidebar 整合 | `AppSidebar.vue` vs `components/sidebar/Sidebar.vue` 二選一 | 無 |
| Auth middleware | 補 JWT validation，讓 ecc dispatch 的 auth check 不再是 no-op | 無 |
| CLAUDE.md 更新 | 移除已修復的 Known Problems，加入新的 checklist | — |

### P2 — 架構演進

| 項目 | 說明 |
|------|------|
| Adapter integration | 把 safe runner 包進 `ClaudeLocalAdapter` |
| Real Claude execution | 用 env flag 控制，safe runner 仍為 default |
| Persistence hardening | SQLite fallback → production Postgres path |

### P3 — 遠期

- PR/CI automation
- Session resume (Paperclip-style)
- Multi-harness (Codex/Cursor/Gemini)
- Redis queue

---

## 今日 Commits

```
b565099 2026-06-02 fix: P0 hardening — fail-fast lifespan, TOCTOU lock, dead code removal
8eded9d 2026-06-01 fix: resolve timezone import issue and deprecation warning
3f9f8a7 2026-06-01 fix: resolve P0 issues - DB init warning and empty board
1a74c72 2026-06-01 feat: implement adapter pattern for AI harness abstraction
```

---

## Remaining Risks

1. **Remote 未設定** — `git push` 需要先 `git remote add origin <url>`
2. **Playwright 未安裝** — E2E test gate 仍不可用
3. **Auth 空操作** — production 部署前必須補 JWT middleware
4. **Redis placeholder** — `/health/ready` 的 redis check 是 hardcoded `"ok"`
