# 2026-06-13 Daily Log

> 週六。今天是個「Plan J」多租戶 + RBAC 集中攻堅日：J-1（DB schema）→ J-2（auth deps / tenant scope）→ J-3（19 個 RBAC gate 上線）→ J-4（frontend permissions 落地），四個 worker 接力，把 backend 從單一 admin 模式帶進多租戶 + 角色驅動的世界。
> 主要工程範圍：`/Users/user/Code/kanban`（內部代號 DevFlow，對外是「Hermes 看板」）。

---

## 今日成果

### 規模

```
commits:      14
insertions:   +4,518
deletions:    -113
淨成長:       +4,405 行
files changed: 35

按類型（14 個 commit）：
  feat  9    test  3
  fix   2    docs  1

按 Plan（J = 今日主軸）：
  j-1              3   schema + seed + lifespan wire
  j-2              4   auth deps + tenant scope + AGENTS note + pytest
  j-3              2   codemod + 19 gates wire
  j-2-baseline-collect  1  import fix（被 J-3 codemod 觸發）
  j-4              1   frontend permissions + login + settings gate
  5-lane           2   unblock endpoint + paused sync + lane/role assertions
  auth             1   useAuth dev bypass fix（Plan H）
```

### 主題分組（按完工順序）

#### 1. 5-lane 收尾（凌晨尾巴，03:29）
- `29607b9` feat(5-lane): unblock endpoint + paused sync + mavis auto cycle report
- `a23f2ed` test(5-lane): 8 → 9 lane / role assertions（Plan C mavis lane）

昨日 Plan G/F 收完後的最後一里：unblock endpoint、paused sync、mavis lane 的 lane/role 測試對齊到 9 條。

#### 2. Plan H — useAuth dev bypass fix（04:39）
- `e2a5fc9` fix(auth): useAuth hits `/me` even when no cookie (dev bypass)

LLM provider 設定頁一直顯示 "Login to configure providers."，即使 dev mode 也是。原因是 `useAuth` 看到沒 cookie 就直接 early return，沒去 call `/me`。改成無 cookie 也試 `/me`，吃到 `DEV_AUTH_BYPASS` 的 leader。

#### 3. Plan J phase 1 — DB schema 多租戶（06:11 ~ 06:48，4 個 commit）
- `2f53d3f` feat(j-1): add tenants tables + tenant_id on User and 14 main tables
  - 3 個新表：`tenants` / `tenant_memberships` / `tenant_audits`
  - `users` 多了 4 個欄位：`tenant_id` / `is_super_admin` / `last_tenant_switch_at` / `role` 收緊到 NOT NULL default=user
  - 14 個主表（issues / agents / webhook_events / llm_provider_configs / audit_logs / ecc_jobs / cycle_reports / agent_roles / issue_events / issue_comments / issue_artifacts / issue_handoffs / agent_workers / agent_runs / agent_sessions）都加 nullable `tenant_id` 索引
  - `auth.register` 自動把新帳號掛到 `tnt_default`，讓 J-2 tenant invariant (`tenant_id NOT NULL`) 不靠 registration payload 撐
  - IssueComment.updated_at 還原（昨日 board_id edit 誤刪，影響 test_board_isolation）
- `19ebaf8` feat(j-1): alembic 0021 — tenants + tenant_id on existing tables
  - Dialect-aware 0021，SQLite/Postgres 共用 chain
  - 自動 seed `tnt_default`（slug=default, plan=pro）讓 727 個既有 test 找得到資料
  - users.role 從 NULL/member 倒成 'user'，再 `batch_alter_table` 加 NOT NULL
- `077b030` feat(j-1): dev seed — tnt_default tenant + 4 dev accounts
  - Idempotent seed 腳本：1 tenant + 4 user（superadmin / admin@default / ops@default / user@default，全密碼 `dev123!`）
- `7d491b6` feat(j-1): wire seed_dev_tenants into FastAPI lifespan
  - 把 seed 從「手動跑 / docker exec」拉到 lifespan 自動觸發
  - 解決 DOD 問題：fresh `docker compose up -d backend` 不再需要額外手動 seed

#### 4. Plan J phase 2 — auth deps / tenant scope（12:21 ~ 13:05，4 個 commit）
- `4a6743d` feat(j-2): auth_deps + auth + repository + tenant_scope + middleware
  - `auth_deps.py`: `require_ops` / `require_super_admin` / `require_role` factory / `require_same_tenant`；`require_admin` 加 super_admin 短路（cross-tenant leader 直通）
  - `auth.py`: JWT payload 加 `tenant_id` / `is_super_admin`；`/auth/me` response 加 `role` / `isSuperAdmin` / `tenant` / `permissions`；`DEV_AUTH_BYPASS_ROLE` / `DEV_AUTH_BYPASS_SUPER_ADMIN` env 切換
  - `repository.py`: `TenantScopedRepository` class（get / list 自動帶 tenant_id filter）
  - `tenant_scope.py`: SQLAlchemy `do_orm_execute` event listener 自動注入 tenant_id filter（super_admin 跳過）
  - `middleware/tenant_scope_middleware.py`: `TenantContextMiddleware` 從 request context 設 contextvars、request 結束清掉
  - `main.py`: 註冊 middleware + lifespan
  - `0022_per_tenant_unique_constraints`: UNIQUE 約束防止同 user 在同 tenant 重複 membership
  - **Note**: J-2 worker functional 已就緒但 process 卡在 commit step，由 J-3 session 接手 commit
- `9760f59` docs(j-2): AGENTS.md — DEV_AUTH_BYPASS verifier trap notes
  - 把 J-1 lessons-learned 寫進 AGENTS.md：dev backend container 預設 `DEV_AUTH_BYPASS=true` 會 short-circuit `verify_jwt_token`，curl 200+JWT 不代表真實 password_hash path 有跑
  - 純 notes，沒改既有 rules
- `79b2259` test(j-2): tenant scope listener + super_admin bypass pytest（512 行，5 條）
- `f07c3a1` test(j-2): fix pytest to use snake_case `/me` + inline SQL + AsyncClient
  - 5 條測試中 3 條寫法上炸：
    1. import path `backend.alembic.versions._0021_backfill` 不存在 → 改 inline SQL
    2. `/auth/me` 期待 camelCase → 改 snake_case
    3. AsyncClient fixture 缺 `await`

#### 5. Plan J phase 3 — 19 個 RBAC gate 上線（13:06 ~ 15:04，3 個 commit）
- `872bbf5` feat(j-3): codemod apply_rbac_gates — install 19 RBAC gates
  - 自動把 J-3 prompt §九 對照表（19 個 endpoint 散在 9 個 router）的 RBAC gate 套到既有 function signature
  - import try/except 隔離 J-2 surface，J-2 還沒落地時 codemod 是 no-op（不 crash）
  - 用法：`apply_rbac_gates.py --dry-run | --apply | --verify-only`
- `50e5627` feat(j-3): wire 19 RBAC gates + 6 tenant endpoints + AI studio gate
  - `Annotated[..., Depends(require_role/ops/admin/super_admin)]` 套到 19 個既有 endpoint
  - 6 個新 `/tenants/{id}/{invite,members,role,delete}` + `/tenants` list/create endpoint
  - `/ai_studio/conversations` RBAC gate
  - 727 baseline + 5 new J-2 tests collected（732 total）
  - 5 J-2 tests 4/5 pass；1 fail（`test_register_attaches_default_tenant`）是 SQLAlchemy greenlet fixture issue，deferred
- `41b452b` fix(j-2-baseline-collect): add missing typing.Annotated import to llm.py
  - J-3 codemod 在 `update_provider_config` 加了 `_gate: Annotated[None, Depends(require_role(...))]` 標註，但 forgot import `Annotated`
  - 21 個 test file collection 炸 `NameError: name 'Annotated' is not defined`

#### 6. Plan J phase 4 — frontend permissions & UI gate（16:00）
- `e89de13` feat(j-4): useAuth RBAC fields + usePermissions + useTenant + login page + settings gate
  - `useAuth.ts` 加 `isAdmin` / `isOps` / `isUser` / `isSuperAdmin` / `currentTenantId` / `currentTenantSlug` computed + 改 `fetchRole` 帶 `tenantId/isSuperAdmin/permissions`
  - `usePermissions.ts` 新 — `canManageTenant` / `canConfigureLLM` / `canCreateBoard` / `canCreateIssue` / `canDeleteIssue` / `canDispatch` / `canViewAudit` / `canEditAgentRole` / `canDeleteAIConversation`
  - `useTenant.ts` 新 — `switchTenant()` 預備介面（MVP 不做 switcher）
  - `pages/login.vue` 新 — 簡單 username + password form，call `POST /api/v1/auth/login`，存 token 到 cookie
  - `pages/settings/index.vue` 6 處 `isAdmin` gate 改用 `usePermissions`（canConfigureLLM / canManageTenant / canCreateBoard 等）
  - **Typecheck**: 部分既有 issue（Issue model 缺 isArchived/archivedAt）— 跟 J-4 RBAC 改動無關，defer 到後續 plan

---

## Gate 狀態

| Gate | 結果 | 證據 |
|------|------|------|
| `audit_schema_drift.py` | ✅ **OK** — 22 tables，零 drift | 本次 cron 跑（`AUDIT_DATABASE_URL=...127.0.0.1:5433...`） |
| pytest | 未跑（commit msg 自報 732 collected, 4/5 J-2 通過） | 50e5627 |
| Playwright smoke | 未跑 | — |
| build | 未跑 | — |

audit 跑法（給 user 之後自己驗）：
```bash
cd /Users/user/Code/kanban
AUDIT_DATABASE_URL="postgresql+psycopg2://devflow:devflow_secret@127.0.0.1:5433/devflow" \
  python3 backend/scripts/audit_schema_drift.py
```

建議下班前再手動驗一次 `pytest`（特別是 J-2 那 5 條 tenant scope）+ `npm run e2e:smoke`，再決定要不要推 origin。

---

## Known Problems 更新（對照 `AGENTS.md`）

| 昨日列出 | 今日狀態 |
|---------|---------|
| `issue_comments.extra_data` drift | ✅ 昨日已解；AGENTS.md 該段尚未更新（後續 PR 補上） |
| `issue_handoffs.{decision, review_comment, reviewed_at, reviewed_by}` drift | ✅ 昨日已解；AGENTS.md 同上 |
| Audit 不在 CI 裡 | ✅ 昨日已上 |
| E2E / Playwright 沒落地 | ✅ 昨日已上 |
| `python-jose` 缺失（user profile 提到的） | 未動 |
| DEV_AUTH_BYPASS verifier trap 沒文件 | ✅ `9760f59` 寫進 AGENTS.md |
| 多租戶 / RBAC 缺 | ✅ Plan J 全套上線（剩 1 個 tenant test fixture 還有 greenlet issue） |

新浮現的：
- **多租戶 fixture 問題** — `test_register_attaches_default_tenant` 因 SQLAlchemy greenlet fixture 報錯，J-2 5 條中唯一 1 fail，deferred
- **Type drift** — `Issue` model 缺 `isArchived` / `archivedAt` field，frontend typecheck 抱怨，J-4 沒處理
- **Tenant switcher** — `useTenant.switchTenant()` 只做介面沒接 API，下一步要先做 endpoint
- **沒推 origin** — 14 commits 全在 main 本地

---

## 未提交工作（uncommitted）⚠️

依照 user 偏好「每次執行完畢記得 git」：

```bash
$ git status --short
?? backend/devflow.db.pre-j1.bak
?? deliveries/
```

- `backend/devflow.db.pre-j1.bak` — J-1 schema 變更前的 SQLite 備份，理論上不再需要，可清掉
- `deliveries/` — 昨日 Plan D 產物（`snake.html`）+ 預期會陸續進來的新 artifact；建議加 `.gitkeep` 或直接 commit

**其他專案**（掃過所有 `/Users/user/Code/*` 下的 git repo）：

| Repo | 狀態 |
|------|------|
| `HackingTools` | 3 個改動（`hackingtool.py` / `requirements.txt` / 新 `tools/threat_intel.py`）|
| `SecurityWeb` | 11 個改動 — 含 1 個 otx.ts 修改、`.claude/plan/*.md`、2 張截圖 |
| `docker-drawio` | 2 個改動（docker-compose.yml + .env）|

---

## 今日 Commits（完整列表，最舊 → 最新）

```
29607b9  feat(5-lane): unblock endpoint + paused sync + mavis auto cycle report
a23f2ed  test(5-lane): 8 -> 9 lane / role assertions (Plan C mavis)
e2a5fc9  fix(auth): useAuth hits /me even when no cookie (dev bypass)
2f53d3f  feat(j-1): add tenants tables + tenant_id on User and 14 main tables
19ebaf8  feat(j-1): alembic 0021 — tenants + tenant_id on existing tables
077b030  feat(j-1): dev seed — tnt_default tenant + 4 dev accounts
7d491b6  feat(j-1): wire seed_dev_tenants into FastAPI lifespan
4a6743d  feat(j-2): auth_deps + auth + repository + tenant_scope + middleware
9760f59  docs(j-2): AGENTS.md — DEV_AUTH_BYPASS verifier trap notes
79b2259  test(j-2): tenant scope listener + super_admin bypass pytest
f07c3a1  test(j-2): fix pytest to use snake_case /me + inline SQL + AsyncClient
872bbf5  feat(j-3): codemod apply_rbac_gates — install 19 RBAC gates
50e5627  feat(j-3): wire 19 RBAC gates + 6 tenant endpoints + AI studio gate
41b452b  fix(j-2-baseline-collect): add missing typing.Annotated import to llm.py
e89de13  feat(j-4): useAuth RBAC fields + usePermissions + useTenant + login page + settings gate
```

(共 14 個 commit，這份 log 算第 15 個 commit 收尾)

---

## 下一步建議

### 立即（下班前清掉）
1. **`backend/devflow.db.pre-j1.bak`** — J-1 前備份，已不需要，可 trash
2. **`deliveries/`** — 看你要 commit 進去（artifact 也算 source of truth）還是加 `.gitignore` 規則
3. **決定要不要推 origin** — 14 commits 在 main 本地，CI 沒跑過之前先驗證
4. **J-2 那 1 個 fail pytest** — `test_register_attaches_default_tenant` 的 greenlet fixture 問題可以花 30 分鐘修掉，不修就下個 plan 接

### 短期（Plan K 候選）
- 補 `Issue.isArchived` / `archivedAt` model field（解 frontend typecheck 報怨）
- 補 `useTenant.switchTenant()` 後端（`POST /tenants/{id}/switch` + 重新發 JWT）
- 補剩下 18 個有對應 endpoint 但沒 pytest 的 RBAC gate（19 gates 都該有 happy path / 403 path）
- 把 AGENTS.md「Known historical drift (resolved)」段更新成包含昨天 + 今天的 schema 變更

### 已被這批 commit 解決的（可以從 CLAUDE.md Known Problems 移除）
- DEV_AUTH_BYPASS verifier trap 沒文件 — `9760f59` 寫進 AGENTS.md
- 多租戶 / RBAC 缺 — Plan J 全套
- useAuth dev bypass 看不到 leader — `e2a5fc9`

---

## Remaining Risks
1. **沒跑 pytest** — 14 個 commit 沒有人親自跑過完整 suite，CI 沒擋
2. **沒推 origin** — 14 commits 留在本地
3. **J-2 fixture 1 條 fail** — `test_register_attaches_default_tenant` 沒修
4. **Type drift** — Issue model 缺 `isArchived` field，frontend typecheck 還在抱怨
5. **其他專案的 uncommitted** — SecurityWeb 11 個、HackingTools 3 個、docker-drawio 2 個都沒收
6. **Tenant switcher** — 前端介面有了，後端 endpoint 還沒做，user 切換 tenant 還是要重登入
7. **`python-jose` 缺漏** — 已被 PyJWT-first fallback 繞過，但根本問題（image 沒裝 + requirements 宣告）還在
